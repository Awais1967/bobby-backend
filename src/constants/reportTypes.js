const REPORT_GROUP_BY = Object.freeze({
  DAY: "day",
  WEEK: "week",
  MONTH: "month",
});

const EXPORT_FORMAT = Object.freeze({
  CSV: "csv",
  EXCEL: "excel",
});

module.exports = {
  EXPORT_FORMAT,
  REPORT_GROUP_BY,
};
